// ltl-langton-ant/main.ltl
//
// Langton's ant on an infinite torus grid.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-langton-ant v0.1.0")
    io::println("Langton's ant on an infinite torus grid.")
    0
}
