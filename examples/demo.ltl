// examples/demo.ltl — runnable demo for ltl_langton_ant

import std::io
use ltl_langton_ant as lib

fn main() {
    io::println("ltl_langton_ant demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}